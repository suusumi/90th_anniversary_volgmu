import {createTheme} from "@mui/material";

export const theme = createTheme({
    typography: {
        fontFamily: "Manrope, sans-serif",
        fontWeightRegular: 400, // Обычный текст
        fontWeightBold: 700, // Жирный текст
    },
    palette: {
        primary: {
            main: "#3DCC6F",

        }
    },

});